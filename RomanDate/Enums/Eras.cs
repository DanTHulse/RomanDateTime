namespace RomanDate.Enums
{
    /// <summary>
    /// Enum representing the different eras used to determine the year
    /// </summary>
    public enum Eras
    {
        AD = 0,
        BC = 1
    }
}