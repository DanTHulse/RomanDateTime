namespace RomanDate.Enums
{
    /// <summary>
    /// Enum used to represent the sacred days (set days) used in the Roman calendar
    /// </summary>
    public enum SetDays
    {
        Kalendae = 1,
        Nonae = 2,
        Idus = 3
    }
}
